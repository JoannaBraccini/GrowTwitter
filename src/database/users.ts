import { User } from "../class/User";

export const users: User[] = [];
/**
  private _name: string;
  private _username: string;
  private _email: string;
  private _password: string;
  private _followed: User[];
 */
